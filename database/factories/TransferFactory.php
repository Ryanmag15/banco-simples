<?php

namespace Database\Factories;

use Illuminate\Database\Eloquent\Factories\Factory;

/**
 * Factory para a model Transfer.
 *
 * Esta factory é utilizada para gerar dados fictícios para testes ou seeds
 * da model \App\Models\Transfer.
 *
 * @extends \Illuminate\Database\Eloquent\Factories\Factory<\App\Models\Transfer>
 */
class TransferFactory extends Factory
{
    /**
     * Define o estado padrão do modelo.
     *
     * @return array<string, mixed> Os atributos padrão para o modelo Transfer.
     */
    public function definition(): array
    {
        return [
            // Adicione os campos fictícios conforme a estrutura da model Transfer
        ];
    }
}
``
